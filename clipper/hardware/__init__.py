"""clipper.hardware — Hardware action modules.

Each sub-module registers its actions into clipper.actions.registry on import.
Import this package (or a specific sub-module) from wherever actions need
to be available at startup (e.g. clipper/actions.py already imports device).
"""
